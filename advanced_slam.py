# Advanced - Pose Estimation + Optimization
def estimate_pose():
    # robot ki location batata hai
    x, y, angle = 1.5, 2.0, 90
    print(f"Robot ab is jagah hai: x={x}, y={y}")
    return x, y

estimate_pose()
